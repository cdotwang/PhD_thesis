#let acknowledgement(body) = {
    heading(level: 1, numbering: none)[Acknowledgements]
    body
}

#let publication(body) = {
    heading(level: 1, numbering: none)[List of publications]
    body
}

#let about(body) = {
    heading(level: 1, numbering: none)[About the author]
    body
}
